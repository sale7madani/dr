{
  "name": "sonbol-platform",
  "version": "1.0.0",
  "private": true,
  "description": "منصّة سنبل لتوصيل الطعام — واجهات الزبون/المطعم/الكابتن/الإدارة مع ناقل أحداث حيّ",
  "scripts": {
    "start": "node --no-warnings backend/server.js",
    "backend": "node --no-warnings backend/server.js",
    "demo": "node server/server.js",
    "test": "node --no-warnings backend/test.js",
    "itest": "node --no-warnings backend/itest.js"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@babel/standalone": "^7.29.7"
  }
}
